# MongoDB connection string. Example for local:
# MONGO_URI=mongodb://localhost:27017/cbt
# Example for MongoDB Atlas (replace user, pass, cluster, and appName):
# MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>/cbt?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=change_me
PORT=4000
# Comma-separated list or single origin for CORS during dev
CLIENT_URL=http://localhost:5173
