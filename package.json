{
  "name": "cbt",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["client", "server"],
  "scripts": {
    "install:all": "npm --prefix client install && npm --prefix server install",
    "dev": "concurrently --kill-others -n client,server -c green,blue \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start",
    "lint": "npm --prefix client run lint && npm --prefix server run lint",
    "test": "npm --prefix client run test && npm --prefix server run test",
    "test:client": "npm --prefix client run test",
    "test:server": "npm --prefix server run test"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
